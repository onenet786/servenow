# Feature development workflow

---

## Workflow Steps

### [x] Step: Requirements

**Completed** - Created comprehensive PRD with:
- 8 user stories covering all wallet features
- 19 functional requirements
- 8 non-functional requirements  
- Success criteria with test cases

Saved to: `d:\GIT\servenow\.zencoder\chats\049cd69c-c522-4a61-827a-05443cde8d69\requirements.md`

Your job is to generate a Product Requirements Document based on the feature description,

First, analyze the provided feature definition and determine unclear aspects. For unclear aspects: - Make informed guesses based on context and industry standards - Only mark with [NEEDS CLARIFICATION: specific question] if: - The choice significantly impacts feature scope or user experience - Multiple reasonable interpretations exist with different implications - No reasonable default exists - Prioritize clarifications by impact: scope > security/privacy > user experience > technical details

Ask up to 5 most priority clarifications to the user. Then, create the document following this template:

```
# Feature Specification: [FEATURE NAME]


## User Stories*


### User Story 1 - [Brief Title]

**Acceptance Scenarios**:

1. **Given** [initial state], **When** [action], **Then** [expected outcome]
2. **Given** [initial state], **When** [action], **Then** [expected outcome]

---

## Requirements*

## Success Criteria*

```

Save the PRD into `d:\GIT\servenow\.zencoder\chats\049cd69c-c522-4a61-827a-05443cde8d69/requirements.md`.

### [x] Step: Technical Specification

**Completed** - Created detailed technical specification covering:
- Technical context (Flutter, Provider, Stripe integration)
- Implementation decisions (state management, API patterns)
- Source code structure (files to create/modify)
- Complete data model contracts
- API endpoint definitions
- 6 delivery phases with incremental testing

Saved to: `d:\GIT\servenow\.zencoder\chats\049cd69c-c522-4a61-827a-05443cde8d69/spec.md`

Based on the PRD in `d:\GIT\servenow\.zencoder\chats\049cd69c-c522-4a61-827a-05443cde8d69/requirements.md`, create a detailed technical specification to be used by a coding agent to implement the feature. Follow this template:

```
# Technical Specification: [FEATURE]

## Technical Context
Language/Version, primary dependencies, etc

## Technical Implementation Brief

Summarize key technical decisions for implementing the feature. Make sure they take into account the existing code as much as possible.

## Source Code Structure

## Contracts

Define addition or changes in data models, DB schemas, APIs, code interfaces etc

## Delivery Phases

Define several incremental deliverables for the feature. Each should be a minimal viable product testable end-to-end.

## Verification Strategy

Define how the coding agent can verify each deliverable it creates. Provide instructions for the agent to perform the verification using available tools (lint/test commands, bash commands) and create helper scripts and tools for more complex result verification.
The verification for each deliverable should be executable by a coding agent using built-in capabilities (lint and test commands from the project, bash commands), pre-generated helper scripts or MCP servers. Research and add to the spec:

- MCP servers that should be installed to help the agent with the verification

- helper scripts that need to be generated in the first phases of the plan to verify complex scenarios that can't be covered by the tests in the project's test framework(s)

- any sample input artifact(s) that are required for verification. Note if these artifacts can be a) generated by the agent; b) discovered by the agent on line; c) must be provided by the user.
```

Save the spec to `d:\GIT\servenow\.zencoder\chats\049cd69c-c522-4a61-827a-05443cde8d69/spec.md`.

### [ ] Step: Implementation Plan

**Completed** - Detailed implementation tasks created below organized by delivery phase.

---

## PHASE 1: Foundation & Wallet Balance (MVP)

### [ ] Step 1.1: Setup Dependencies & Initial Scaffold
**Task**: Add flutter_stripe and intl dependencies; create initial file structure

**Contracts**:
- Dependencies: flutter_stripe, intl
- Files to create: models/, providers/, widgets/, screens/wallet_screen.dart

**Deliverables**:
1. pubspec.yaml updated with flutter_stripe and intl
2. Directory structure created
3. Basic WalletScreen placeholder created
4. Route added to main.dart

**Verification**:
```bash
flutter pub get
flutter analyze  # Should have no errors related to new packages
grep "flutter_stripe" pubspec.yaml
```

---

### [ ] Step 1.2: Create Wallet Data Models
**Task**: Implement WalletModel, WalletTransactionModel, and basic models

**Contracts** (from spec):
- WalletModel (id, userId, balance, totalCredited, totalSpent, autoRechargeEnabled, autoRechargeAmount, autoRechargeThreshold, etc.)
- WalletTransactionModel (id, walletId, type, amount, description, referenceType, balance_after, createdAt)

**Deliverables**:
1. lib/models/wallet_model.dart
2. lib/models/transaction_model.dart
3. Both models with fromJson/toJson methods

**Verification**:
```bash
# Test JSON serialization
flutter test test/models/wallet_model_test.dart
# Should pass serialization/deserialization tests
```

---

### [ ] Step 1.3: Create WalletProvider with State Management
**Task**: Implement WalletProvider using Provider pattern for wallet balance management

**Contracts** (from spec):
- WalletProvider extends ChangeNotifier
- Methods: loadWalletBalance(), getters for wallet, isLoading, error
- Local caching with SharedPreferences

**Deliverables**:
1. lib/providers/wallet_provider.dart
2. Wallet balance loading logic
3. Error handling
4. Loading state management

**Verification**:
```bash
flutter test test/providers/wallet_provider_test.dart
# Tests should cover:
# - Loading wallet balance
# - State notification on changes
# - Error handling
# - LocalStorage caching
```

---

### [ ] Step 1.4: Extend ApiService with Wallet Endpoints
**Task**: Add wallet API methods to ApiService

**Contracts** (from spec):
- GET /api/wallet/balance
- POST /api/wallet/topup
- All methods use Bearer token auth

**Deliverables**:
1. ApiService.getWalletBalance(token)
2. ApiService.topupWallet(token, {amount, paymentMethodId, ...})
3. ApiService.getWalletTransactions(token, {limit, offset, type})
4. Error handling with proper exception throwing

**Verification**:
```bash
# Verify endpoints exist and have correct signatures
grep -n "getWalletBalance\|topupWallet" lib/services/api_service.dart
# Should find both methods with proper Bearer token handling
```

---

### [ ] Step 1.5: Setup Stripe Integration
**Task**: Initialize Stripe in Flutter app with public key from backend

**Contracts**:
- Initialize flutter_stripe with Stripe.publishableKey
- Get public key from wallet/balance endpoint
- Handle Stripe initialization errors

**Deliverables**:
1. Stripe initialization in AuthProvider (on login)
2. Stripe public key retrieval and storage
3. Error handling for Stripe init failures

**Verification**:
```bash
# Check Stripe initialization in code
grep -n "flutter_stripe\|Stripe.publishableKey" lib/providers/auth_provider.dart
# Should find initialization logic
```

---

### [ ] Step 1.6: Create Wallet Balance Card UI Component
**Task**: Build UI component displaying current balance and quick stats

**Contracts**:
- Display: balance, totalCredited, totalSpent, transactionCount
- Styled card with gradient background
- Refresh button

**Deliverables**:
1. lib/widgets/wallet_balance_card.dart
2. Responsive design for mobile
3. Loading state with shimmer
4. Error state display

**Verification**:
```bash
flutter test test/widgets/wallet_balance_card_test.dart
# Should render correctly with test data
# Should show loading/error states
```

---

### [ ] Step 1.7: Create Topup Form & Card Payment UI
**Task**: Build topup form with quick amount buttons and card element

**Contracts**:
- Amount input with validation (> 0)
- Quick amount buttons (500, 1000, 2000, 5000 PKR)
- Stripe card element via flutter_stripe
- "Save card" checkbox
- Submit button

**Deliverables**:
1. lib/widgets/topup_form.dart
2. Form validation logic
3. Stripe card element integration
4. Error/success message display

**Verification**:
```bash
flutter test test/widgets/topup_form_test.dart
# Should validate amount input
# Should display quick amount buttons
# Should integrate with Stripe card element
```

---

### [ ] Step 1.8: Implement Topup Flow (Form → Stripe → Server)
**Task**: Connect topup form to Stripe payment and server topup API

**Contracts** (from spec):
- POST /api/wallet/topup with amount, paymentMethodId, saveCard
- Create Stripe PaymentMethod from card
- Handle payment success/failure
- Update wallet balance on success

**Deliverables**:
1. handleTopupSubmit() in WalletProvider
2. Stripe payment creation logic
3. Server topup request with payment method
4. Balance refresh after successful topup
5. User-friendly success/error messages

**Verification**:
Manual test with test Stripe card:
```
Card: 4242 4242 4242 4242
Expiry: Any future date
CVC: Any 3 digits
```
- Successful payment should credit wallet
- Failed payment should show error
- Balance should update after success

---

### [ ] Step 1.9: Create Main WalletScreen Structure
**Task**: Build main wallet screen with tabbed interface

**Contracts**:
- TabBar with sections: Balance, Topup, Transfers, History
- Integration with all components
- Navigation back to home

**Deliverables**:
1. lib/screens/wallet_screen.dart (tabbed structure)
2. Tab controller setup
3. Integration with WalletProvider
4. Responsive tab layout

**Verification**:
```bash
flutter run
# Navigate to wallet screen
# Verify all tabs are visible and selectable
# Verify components load without errors
```

---

### [ ] Step 1.10: Add Route to Navigation
**Task**: Add wallet route to main.dart and create navigation button

**Contracts**:
- Add '/wallet' route to named routes
- Update home screen navigation if needed

**Deliverables**:
1. Modified main.dart with /wallet route
2. Navigation from home screen (optional button/drawer item)

**Verification**:
```bash
flutter run
# Should navigate to wallet screen successfully
# Should preserve auth state
```

---

### [ ] Step 1.11: Phase 1 Testing & Verification
**Task**: Comprehensive testing of wallet balance, topup, and Stripe integration

**Deliverables**:
- Manual test cases executed
- Edge cases tested (insufficient balance, network errors, invalid cards)
- Screenshots documented

**Verification Checklist**:
- [ ] Wallet balance displays correctly
- [ ] Topup form validates amount correctly
- [ ] Quick amount buttons work
- [ ] Stripe card element renders
- [ ] Successful payment updates balance
- [ ] Failed payment shows error message
- [ ] Network error handled gracefully
- [ ] Loading states show properly
- [ ] UI responsive on different screen sizes
- [ ] No lint errors: `flutter analyze`

---

## PHASE 2: Transaction History & Auto-Recharge

### [ ] Step 2.1: Create Transaction Models & API Integration
**Task**: Implement transaction loading and filtering

**Contracts** (from spec):
- WalletTransactionModel with type, amount, description, balanceAfter
- GET /api/wallet/transactions with pagination
- Filter by type (credit, debit, refund, transfer)

**Deliverables**:
1. Extended WalletProvider with loadTransactions()
2. Filter logic in WalletProvider
3. ApiService.getWalletTransactions() method
4. Pagination support (limit=20, offset=0)

**Verification**:
```bash
flutter test test/providers/wallet_provider_test.dart
# Should load transactions
# Should filter by type
# Should paginate correctly
```

---

### [ ] Step 2.2: Create Transaction List UI Component
**Task**: Build reusable transaction list item and list view

**Contracts**:
- Display: date, type badge, amount, description, balance after
- Color-coded badges (credit=green, debit=red, etc.)
- Responsive list layout

**Deliverables**:
1. lib/widgets/transaction_list_item.dart
2. List formatting with proper styling
3. Timestamp formatting using intl package

**Verification**:
```bash
flutter test test/widgets/transaction_list_item_test.dart
# Should render transaction data correctly
# Should display badges with correct colors
```

---

### [ ] Step 2.3: Implement Transaction History Tab
**Task**: Add transaction history section to wallet screen with filtering

**Contracts**:
- Display transaction list with pagination
- Filter dropdown (All, Credit, Debit, Refund, Transfer)
- Load more button for pagination
- Empty state message

**Deliverables**:
1. Transactions tab in WalletScreen
2. Filter UI component
3. Load more pagination
4. Transaction list rendering

**Verification**:
```bash
flutter run
# Navigate to transactions tab
# Verify filtering works
# Verify load more works
```

---

### [ ] Step 2.4: Add Auto-Recharge Settings UI
**Task**: Create auto-recharge configuration form

**Contracts**:
- Toggle for enable/disable
- Threshold amount input (required if enabled)
- Recharge amount input (required if enabled)
- Validation: threshold < recharge amount
- Status display (Enabled/Disabled)

**Deliverables**:
1. lib/widgets/auto_recharge_settings.dart
2. Form validation logic
3. Settings display component

**Verification**:
```bash
flutter test test/widgets/auto_recharge_settings_test.dart
# Should validate form inputs
# Should require fields when enabled
# Should validate threshold < amount
```

---

### [ ] Step 2.5: Implement Auto-Recharge API Integration
**Task**: Connect auto-recharge settings to backend

**Contracts** (from spec):
- POST /api/wallet/auto-recharge
- GET /api/wallet/auto-recharge
- WalletProvider methods: loadAutoRechargeSettings(), saveAutoRechargeSettings()

**Deliverables**:
1. ApiService methods for auto-recharge
2. WalletProvider auto-recharge methods
3. Settings loading on screen init
4. Settings persistence after save

**Verification**:
```bash
flutter test test/providers/wallet_provider_test.dart
# Should load settings
# Should save settings
# Should validate inputs
```

---

### [ ] Step 2.6: Phase 2 Testing & Verification
**Task**: Test transaction history, filtering, and auto-recharge features

**Verification Checklist**:
- [ ] Transaction history loads with correct data
- [ ] Filter by type works for all types
- [ ] Load more pagination works
- [ ] Empty state shows when no transactions
- [ ] Auto-recharge form validates correctly
- [ ] Auto-recharge settings can be enabled/disabled
- [ ] Settings persist after save
- [ ] All UI components responsive

---

## PHASE 3: Payment Methods Management

### [ ] Step 3.1: Create PaymentMethodModel
**Task**: Implement payment method data model

**Contracts** (from spec):
- PaymentMethodModel with id, type, cardLastFour, cardBrand, expiry, isPrimary
- fromJson/toJson methods

**Deliverables**:
1. lib/models/payment_method_model.dart

**Verification**:
```bash
flutter test test/models/payment_method_model_test.dart
```

---

### [ ] Step 3.2: Add Payment Methods API Endpoints
**Task**: Extend ApiService with payment method endpoints

**Contracts** (from spec):
- GET /api/wallet/payment-methods
- PUT /api/wallet/payment-methods/:id/primary
- DELETE /api/wallet/payment-methods/:id

**Deliverables**:
1. ApiService methods for payment method operations
2. WalletProvider methods: loadPaymentMethods(), setPrimaryPaymentMethod(), deletePaymentMethod()

**Verification**:
```bash
grep -n "loadPaymentMethods\|setPrimaryPaymentMethod\|deletePaymentMethod" lib/providers/wallet_provider.dart
```

---

### [ ] Step 3.3: Create Payment Methods UI Component
**Task**: Build card list and management interface

**Contracts**:
- Display saved cards with brand, last 4 digits, expiry
- Mark primary card visually
- Delete button
- Set as primary button

**Deliverables**:
1. lib/widgets/payment_method_card.dart
2. Payment methods list view
3. Action buttons (set primary, delete)

**Verification**:
```bash
flutter test test/widgets/payment_method_card_test.dart
```

---

### [ ] Step 3.4: Implement Payment Methods Management
**Task**: Add payment methods section to wallet screen

**Contracts**:
- List saved payment methods
- Delete with confirmation dialog
- Set as primary
- "Save card" during topup must work

**Deliverables**:
1. Payment methods tab in WalletScreen
2. Delete confirmation dialog
3. Success/error messages

**Verification**:
Manual test:
- Save a card during topup
- Verify it appears in saved cards list
- Set as primary
- Delete card (should ask for confirmation)

---

### [ ] Step 3.5: Phase 3 Testing & Verification
**Task**: Test payment method save, list, update, and delete

**Verification Checklist**:
- [ ] Card saved during topup appears in list
- [ ] Card details display correctly (brand, last 4, expiry)
- [ ] Primary card marked visually
- [ ] Can set card as primary
- [ ] Delete card with confirmation
- [ ] Deletion successful
- [ ] Empty state when no saved cards

---

## PHASE 4: P2P Transfers (Send Money)

### [ ] Step 4.1: Create Transfer Models
**Task**: Implement WalletTransferModel

**Contracts** (from spec):
- WalletTransferModel with sender/recipient info, amount, status, dates
- Status: pending, completed, rejected, cancelled

**Deliverables**:
1. lib/models/transfer_model.dart
2. fromJson/toJson methods

**Verification**:
```bash
flutter test test/models/transfer_model_test.dart
```

---

### [ ] Step 4.2: Add Transfer API Endpoints
**Task**: Extend ApiService with transfer endpoints

**Contracts** (from spec):
- POST /api/wallet/transfers/send
- GET /api/wallet/transfers/sent
- GET /api/wallet/transfers/received
- POST /api/wallet/transfers/:id/accept
- POST /api/wallet/transfers/:id/reject
- POST /api/wallet/transfers/:id/cancel

**Deliverables**:
1. ApiService methods for all transfer operations
2. WalletProvider methods: sendMoney(), sendMoneyByEmail(), loadSentTransfers(), etc.

**Verification**:
```bash
grep -n "sendMoney\|loadSentTransfers" lib/services/api_service.dart lib/providers/wallet_provider.dart
```

---

### [ ] Step 4.3: Create Send Money Form
**Task**: Build form for sending money with recipient lookup

**Contracts**:
- Recipient input (by User ID or email)
- Recipient validation/search
- Amount input (must not exceed balance)
- Description (optional)
- Submit button

**Deliverables**:
1. lib/widgets/send_money_form.dart
2. Form validation
3. Recipient lookup logic

**Verification**:
```bash
flutter test test/widgets/send_money_form_test.dart
# Should validate recipient input
# Should validate amount
# Should not allow amount > balance
```

---

### [ ] Step 4.4: Implement Send Money Flow
**Task**: Connect send money form to backend

**Contracts**:
- Call API with recipientId/email, amount, description
- Handle insufficient balance error
- Handle invalid recipient error
- Show success with transfer ID
- Update balance after send

**Deliverables**:
1. handleSendMoney() in WalletProvider
2. Recipient lookup from backend (email → userId)
3. Error handling and user feedback

**Verification**:
Manual test:
- Send money by User ID
- Send money by email
- Verify transfer created with pending status
- Verify balance decremented immediately

---

### [ ] Step 4.5: Create Sent Transfers List UI
**Task**: Build UI for viewing sent transfers with status

**Contracts**:
- Display sent transfers list
- Show: recipient name/email, amount, status, date
- Status badges (pending, completed, rejected)
- Cancel button for pending transfers
- Pagination support

**Deliverables**:
1. lib/widgets/transfer_card.dart
2. Sent transfers tab in WalletScreen
3. Cancel transfer dialog

**Verification**:
```bash
flutter test test/widgets/transfer_card_test.dart
```

---

### [ ] Step 4.6: Implement Transfer Cancellation
**Task**: Allow sender to cancel pending transfers

**Contracts**:
- Only cancel if status = pending
- Restore sender's balance
- Confirmation dialog
- Update UI after cancel

**Deliverables**:
1. Cancel button in transfer card
2. Confirmation dialog
3. Balance restoration

**Verification**:
Manual test:
- Send money to create pending transfer
- Cancel transfer
- Verify balance restored
- Verify status updated to cancelled

---

### [ ] Step 4.7: Phase 4 Testing & Verification
**Task**: Test complete send money flow and transfer management

**Verification Checklist**:
- [ ] Send by User ID works
- [ ] Send by email works
- [ ] Recipient not found error shown
- [ ] Insufficient balance error shown
- [ ] Transfer created with pending status
- [ ] Sender balance updated
- [ ] Sent transfers list displays correctly
- [ ] Can cancel pending transfer
- [ ] Balance restored on cancel
- [ ] All error messages clear

---

## PHASE 5: P2P Transfers (Receive & Accept/Reject)

### [ ] Step 5.1: Create Received Transfers UI
**Task**: Build interface for pending and completed received transfers

**Contracts**:
- Display received transfers with sender info
- Show: sender name/email, amount, status, date
- Accept/reject buttons for pending transfers
- Rejection reason input field

**Deliverables**:
1. Received transfers tab in WalletScreen
2. Separate views for pending and completed transfers
3. Transfer status badges

**Verification**:
```bash
flutter run
# Navigate to received transfers tab
# Verify UI displays correctly
```

---

### [ ] Step 5.2: Implement Transfer Acceptance
**Task**: Accept pending transfer and credit wallet

**Contracts** (from spec):
- POST /api/wallet/transfers/:id/accept
- Verify sender still has balance
- Transfer amount from sender to recipient
- Update both wallets
- Create transaction records
- Update transfer status to completed

**Deliverables**:
1. handleAcceptTransfer() in WalletProvider
2. Acceptance logic with error handling
3. User confirmation dialog
4. Success message with new balance

**Verification**:
Manual test:
- Have another user send money
- Accept transfer
- Verify amount credited
- Verify transfer status = completed
- Check transaction history shows transfer credit

---

### [ ] Step 5.3: Implement Transfer Rejection
**Task**: Reject pending transfer and restore sender's balance

**Contracts** (from spec):
- POST /api/wallet/transfers/:id/reject
- Optional rejection reason
- Restore sender's balance
- Update transfer status to rejected
- Delete pending debit transaction

**Deliverables**:
1. handleRejectTransfer() in WalletProvider
2. Rejection reason input
3. Confirmation dialog
4. Balance restoration logic

**Verification**:
Manual test:
- Have another user send money
- Reject with/without reason
- Verify sender's balance restored
- Verify transfer status = rejected
- Verify reason saved (if provided)

---

### [ ] Step 5.4: Implement Transfer History Display
**Task**: Show history of all sent and received transfers

**Contracts**:
- Separate tabs for sent and received
- Display transfer list with status
- Pagination support (limit=20, offset)
- Filter by status (pending, completed, rejected)

**Deliverables**:
1. Transfer history views in tabs
2. Transfer card display with details
3. Pagination/load more
4. Optional status filtering

**Verification**:
```bash
flutter run
# Check sent and received transfer history
# Verify completed, rejected, pending transfers display
```

---

### [ ] Step 5.5: Phase 5 Testing & Verification
**Task**: Test complete receive transfer flow

**Verification Checklist**:
- [ ] Received transfers list shows pending transfers
- [ ] Accept transfer credits wallet
- [ ] Reject transfer restores sender balance
- [ ] Transfer status updates correctly
- [ ] Rejection reason saved
- [ ] Transfer history shows completed/rejected
- [ ] Confirmation dialogs work
- [ ] Success messages clear
- [ ] All error cases handled

---

## PHASE 6: Polish, Testing & Documentation

### [ ] Step 6.1: Unit Tests for WalletProvider
**Task**: Write comprehensive unit tests for state management

**Deliverables**:
1. test/providers/wallet_provider_test.dart with tests for:
   - loadWalletBalance()
   - loadTransactions() with filtering
   - sendMoney()
   - acceptTransfer()
   - rejectTransfer()
   - auto-recharge settings
   - state updates and notifications

**Verification**:
```bash
flutter test test/providers/wallet_provider_test.dart
# Should achieve 80%+ coverage
# All tests should pass
```

---

### [ ] Step 6.2: Widget Tests
**Task**: Write widget tests for UI components

**Deliverables**:
1. test/widgets/ directory with tests for:
   - wallet_balance_card_test.dart
   - topup_form_test.dart
   - transaction_list_item_test.dart
   - transfer_card_test.dart
   - send_money_form_test.dart

**Verification**:
```bash
flutter test test/widgets/
# All widget tests should pass
```

---

### [ ] Step 6.3: Integration Tests
**Task**: Write integration tests for complete flows

**Deliverables**:
1. test_driver/integration_test.dart with tests for:
   - Complete topup flow
   - Transaction history loading
   - Send money and acceptance
   - Auto-recharge configuration

**Verification**:
```bash
flutter drive --target=test_driver/integration_test.dart
# Should complete successfully
```

---

### [ ] Step 6.4: Error Handling & Edge Cases
**Task**: Comprehensive error handling and edge case testing

**Deliverables**:
1. Handle network errors gracefully
2. Handle invalid Stripe tokens
3. Handle insufficient balance
4. Handle user not found errors
5. Handle concurrent operations
6. Test offline mode with cached data

**Verification**:
Manual testing checklist:
- [ ] Network error shown as snackbar
- [ ] Invalid card error shows message
- [ ] Insufficient balance error shown
- [ ] User not found error shown
- [ ] App handles token expiry
- [ ] Offline mode uses cached data

---

### [ ] Step 6.5: Performance Optimization
**Task**: Optimize for performance and smooth UX

**Deliverables**:
1. Implement pagination lazy loading
2. Cache images and data
3. Optimize list rendering
4. Debounce filter operations
5. Use const constructors where applicable

**Verification**:
```bash
flutter run --profile
# Should meet performance benchmarks:
# - Wallet load: < 2 seconds
# - Transaction list: < 3 seconds
# - Topup: < 5 seconds
```

---

### [ ] Step 6.6: Code Quality & Documentation
**Task**: Ensure code quality and add documentation

**Deliverables**:
1. Run flutter analyze (no errors)
2. Run flutter format
3. Add doc comments to public methods
4. Create inline documentation for complex logic

**Verification**:
```bash
flutter analyze
# Should have 0 errors, 0 warnings

dart format lib/ --set-exit-if-changed
# Should format all code

flutter test --coverage
# Should achieve 80%+ coverage
```

---

### [ ] Step 6.7: Final Verification & UAT
**Task**: Complete user acceptance testing

**Verification Checklist**:
- [ ] All features work as per requirements
- [ ] UI is responsive on different screen sizes
- [ ] All error messages are user-friendly
- [ ] Performance meets benchmarks
- [ ] No memory leaks
- [ ] No console errors
- [ ] All tests pass
- [ ] Code quality metrics met
- [ ] Ready for production deployment

---

## Next Steps After Implementation Plan

Once implementation begins, this plan.md will be updated with:
- [ ] Step: Development (implementation of all tasks)
- [ ] Step: Quality Assurance (final testing)
- [ ] Step: Deployment (app store submission)

Based on the technical spec in `d:\GIT\servenow\.zencoder\chats\049cd69c-c522-4a61-827a-05443cde8d69/spec.md`, create a detailed task plan and update `d:\GIT\servenow\.zencoder\chats\049cd69c-c522-4a61-827a-05443cde8d69/plan.md`. Each task should have task definition, references to contracts to be used/implemented, deliverable definition and verification instructions.

Format each task as

```
### [ ] Step: <task_name>
Task instructions
```
